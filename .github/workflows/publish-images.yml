name: Publish GHCR images

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_TAG: ${{ github.ref_name }}

jobs:
  publish-sdx-controller:
    name: Publish sdx-controller
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/sdx-controller
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/sdx-controller:${{ env.IMAGE_TAG }}

  publish-sdx-lc:
    name: Publish sdx-lc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/sdx-lc
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/sdx-lc:${{ env.IMAGE_TAG }}

  publish-sdx-oxp-integrator:
    name: Publish sdx-oxp-integrator
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/sdx-oxp-integrator
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/sdx-oxp-integrator:${{ env.IMAGE_TAG }}

  publish-kytos-sdx:
    name: Publish kytos-sdx
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/kytos-sdx
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/kytos-sdx:${{ env.IMAGE_TAG }}

  publish-oess-sdx:
    name: Publish oess-sdx
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/oess-sdx
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/oess-sdx:${{ env.IMAGE_TAG }}

  publish-sdx-meican:
    name: Publish sdx-meican
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: services/sdx-meican/docker_for_build
          file: services/sdx-meican/docker_for_build/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/atlanticwave-sdx/sdx-meican:${{ env.IMAGE_TAG }}

